VITE_API_BASE_URL=http://localhost:5000/api
# Production domain used for canonical URLs, Open Graph/Twitter tags, JSON-LD,
# and the generated sitemap.xml. Set this to the live domain in production.
VITE_SITE_URL=https://filatoco.ca
